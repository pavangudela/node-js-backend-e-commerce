import path from 'node:path';

export const config = path.resolve('src', 'config', 'sequelize-cli.js');
export const modelsPath = path.resolve('src', 'models');
export const seedersPath = path.resolve('src', 'seeders');
export const migrationsPath = path.resolve('src', 'migrations');
